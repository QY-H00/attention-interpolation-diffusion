#PBS -S /bin/bash
#PBS -l select=1:ncpus=6:mem=16gb:ngpus=1

cd ~/
nvidia-smi
cd attention-interpolation-diffusion
source ~/.bashrc
eval "$(conda shell.bash hook)"
conda activate AID
python refinement_eval.py --method_name attention --early sfused